# Orchestrator Complete

Open this folder in VS Code and press F5 to run the Extension Development Host.
Use the command "Orchestrator: Open" to open the orchestration UI.
Place YAML workflows in .orchestrator/workflows/\*.yml
Logs and run history are persisted to .logs/
