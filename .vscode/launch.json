{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Node (with prelaunch CI)",
      "type": "pwa-node",
      "request": "launch",
      "program": "${workspaceFolder}/src/index.js",
      "preLaunchTask": "ci:local",
      "cwd": "${workspaceFolder}"
    }
  ],
  "compounds": []
}
