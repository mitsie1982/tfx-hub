  tfma-gate-and-tests:
    name: TFMA Gate and Unit Tests
    runs-on: ubuntu-latest
    needs: build-and-validate
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install test deps
        run: |
          python -m pip install --upgrade pip
          pip install pytest

      - name: Run TFMA gate unit tests
        run: |
          pytest -q tests/test_tfma_gate.py

      - name: Authenticate to GCP (if TFMA evaluation needs GCP)
        if: ${{ secrets.GCP_SA_KEY != '' }}
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Run TFMA evaluation (produce metrics)
        env:
          TFMA_OUTPUT_DIR: tfma-output
        run: |
          mkdir -p "${TFMA_OUTPUT_DIR}"
            # TODO: Replace the following line with your real TFMA evaluation command that writes metrics to ${{ env.TFMA_OUTPUT_DIR }}
            # Example:
            # python my_tfma_eval.py --output_dir ${{ env.TFMA_OUTPUT_DIR }}
            echo '{"overall": {"accuracy": 0.95, "auc": 0.90}}' > "${TFMA_OUTPUT_DIR}/tfma_metrics.json"
        # If your TFMA evaluation runs in a container or on GCP, replace this step accordingly.

      - name: Run TFMA gating script
        env:
          TFMA_OUTPUT_DIR: tfma-output
          # Provide thresholds via secret TFMA_THRESHOLDS or set here as a fallback
          TFMA_THRESHOLDS: ${{ secrets.TFMA_THRESHOLDS }}
        run: |
          python scripts/tfma_gate.py --tfma-dir "${TFMA_OUTPUT_DIR}" --thresholds "${TFMA_THRESHOLDS}"
name: TFX CI - TFMA gating and image build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  IMAGE: gcr.io/my-gcp-project/tfx-model
  IMAGE_TAG: ${{ github.sha }}
  TFMA_OUTPUT_DIR: tfma-output

jobs:
  build-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Configure Docker auth for GCR
        uses: docker/login-action@v2
        with:
          registry: gcr.io
          username: _json_key
          password: ${{ secrets.GCP_SA_KEY }}
        - name: Configure Docker auth
          uses: docker/login-action@v2
          with:
            registry: ${{ secrets.REGISTRY_HOST }}
            username: ${{ secrets.REGISTRY_USERNAME }}
            password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tfx tensorflow-data-validation tensorflow-model-analysis

      - name: Run TFDV (data validation)
        run: |
          mkdir -p tfdv-output
          # adapt input path to your dataset
          echo "Running TFDV (placeholder)"; true

      - name: Run TFMA evaluation (produce metrics)
        run: |
          mkdir -p ${{ env.TFMA_OUTPUT_DIR }}
          # Run your TFMA evaluation here and write metrics to ${{ env.TFMA_OUTPUT_DIR }}
          echo '{"overall": {"accuracy": 0.95, "auc": 0.90}}' > ${{ env.TFMA_OUTPUT_DIR }}/tfma_metrics.json

      - name: Run TFMA gating
        env:
          TFMA_THRESHOLDS: '{"accuracy": 0.90, "auc": 0.85}'
        run: |
          python scripts/tfma_gate.py --tfma-dir ${{ env.TFMA_OUTPUT_DIR }}

      - name: Build Docker image
        run: |
          docker build -t "${{ env.IMAGE }}:${{ env.IMAGE_TAG }}" .
          docker push "${{ env.IMAGE }}:${{ env.IMAGE_TAG }}"

  integration-tests:
    needs: build-and-validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run integration smoke tests
        run: |
          echo "Run integration tests (placeholder)"
