const http = require("http");
const url = require("url");
const port = process.env.PORT || 3002;
function sendHtml(res, html){ res.writeHead(200, {"Content-Type":"text/html; charset=utf-8"}); res.end(html); }
function sendJson(res, obj){ res.writeHead(200, {"Content-Type":"application/json; charset=utf-8"}); res.end(JSON.stringify(obj)); }
const html = `<!doctype html><html><head><meta charset="utf-8"/><title>AMS Demo</title><meta name="viewport" content="width=device-width,initial-scale=1"/><style>body{font-family:Segoe UI,Arial;margin:0;background:#f0fff4;color:#222}.wrap{max-width:980px;margin:48px auto;padding:24px;background:#fff;border-radius:8px;box-shadow:0 6px 18px rgba(0,0,0,.06)}h1{margin:0 0 8px}.meta{color:#666;margin-bottom:16px}table{width:100%;border-collapse:collapse}th,td{padding:8px;border:1px solid #eee;text-align:left}footer{margin-top:18px;color:#888;font-size:13px}</style></head><body><div class="wrap"><h1>AMS Demo</h1><div class="meta">Status: <strong id="status">starting</strong> • Keys: <span id="keys">—</span></div><h3>Active Services</h3><table id="services"><thead><tr><th>Service</th><th>Status</th></tr></thead><tbody></tbody></table><footer>Local demo served on port ${port}</footer></div><script>async function refresh(){try{const r=await fetch('/api/status');const j=await r.json();document.getElementById('status').textContent=j.status;document.getElementById('keys').textContent=j.keys.join(', ');const tbody=document.querySelector('#services tbody');tbody.innerHTML=j.services.map(s=>\`<tr><td>\${s.name}</td><td>\${s.status}</td></tr>\`).join('');}catch(e){document.getElementById('status').textContent='error';}}refresh();setInterval(refresh,3500);</script></body></html>`;
const server = http.createServer((req,res)=>{ const u=url.parse(req.url,true); if(u.pathname==='/'||u.pathname==='/index.html'){ sendHtml(res, html); return; } if(u.pathname==='/api/status'){ const payload={ status:'ready', keys:['auth','client'], services:[ {name:'Auth', status:'ok'}, {name:'Billing', status: Math.random()>0.9 ? 'degraded' : 'ok'}, {name:'Notifications', status:'ok'} ] }; sendJson(res,payload); return; } res.writeHead(404,{'Content-Type':'text/plain'}); res.end('Not found'); });
server.listen(port, '127.0.0.1', () => console.log('Listening on', port));
