# Orchestrator

A small VS Code extension that provides an orchestration UI to run shell commands, workspace tasks, install extensions via the `code` CLI, and merge a `deploy/settings.json` into your settings.

Open this folder in VS Code and press F5 to run the Extension Development Host.
